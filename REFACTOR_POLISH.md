### Section 9.11 — 2026-08-07 — Section 4.4 — replacement implementation/investigation/testing/review worker

- Status: complete
- Ownership and files changed: Recovered valid pre-existing Section 4.4 edits in the permitted paths and verified them without changing concurrent Section 4.1–4.3/user work. The owned implementation paths are `static/css/calendar-overlays.css` (24 fallback replacements), `static/css/core/feedback-overlays.css` (4), `static/css/global-foundation.css` (2), `eslint.config.cjs`, `package.json`, `.gitignore`, and `.github/workflows/ci.yml`; `REFACTOR_POLISH.md` was appended only for this handoff. No generated dist artifact was retained. Temporary lint-probe edits were made only in the permitted `eslint.config.cjs` and were fully removed.
- Commits (if any; no pushes): none. No staging, commit, push, deployment, dependency addition, `instance/` access, or main-checkout edit occurred.
- CSS evidence and behavioral result: `git show HEAD:<file> | rg -o -F 'var(--color-primary, #3b82f6)'` measured the required before counts of 24 in `calendar-overlays.css`, 4 in `core/feedback-overlays.css`, and 2 in `global-foundation.css`. The current targeted search and `rg -n -F 'var(--color-primary, #3b82f6)' static/css` both return no matches (exit 0 with no output); the 30 changed occurrences now use the confirmed theme-aware `#0060aa`. Commit `3a8f3706` documents that fallback as the APStudy primary blue. Existing cascade order and unrelated formatting were preserved; the CSS diff is exactly 24/24, 4/4, and 2/2 lines removed/added.
- Lint baseline and enforcement: `eslint.config.cjs:30–31` records the measured backlog of 3,283 warnings: 3,160 `no-undef` and 123 `no-unused-vars`. `eslint.config.cjs:34–36` promotes `no-unused-vars` to error for `eslint.config.cjs` and the shared `static/js/core/**/*.js` subset. The nine core files listed at `eslint.config.cjs:40–52` retain warning severity for their 16 pre-existing `no-unused-vars` warnings; the other nine of the 18 core files are clean and enforced. No unrelated rules changed. `package.json:13` keeps the backlog explicitly bounded with `eslint . --max-warnings 3283`.
- CI and ignore evidence: `.github/workflows/ci.yml:62–67` checks tracked-and-clean `static/js/vendor/dist/sortable-global.js`, `static/js/notes/dist/notes-editor-v17.js`, and `static/js/tasks/dist/task-app.js`. `.gitignore:157–161` keeps `.github` and non-workflow local data ignored while allowing only intended `*.yml`/`*.yaml` files under `.github/workflows/`; `git check-ignore --no-index` confirmed example YAML workflow paths are not ignored while `example.json` and `.github/local-data.json` remain ignored.
- Tests and exact results:
  - Targeted CSS/asset tests, `node --test tests/js/calendar-color-contrast.test.mjs tests/js/frontend-asset-contract.test.mjs tests/js/task-asset-contract.test.mjs` — exit 0; 113 passed, 0 failed/skipped.
  - `npm run lint` — exit 0; 0 errors, 3,283 warnings.
  - Deliberate proof: after adding one unused binding to `eslint.config.cjs`, `zsh -o pipefail -c 'npm run lint 2>&1 | tail -8'` — exit 1; 1 error, 3,283 warnings, with the error reported by `no-unused-vars`. The binding was removed immediately; the same command then exited 0 with 0 errors and 3,283 warnings, and `rg '__section44LintProbe' eslint.config.cjs` returned no match.
  - `npm test` — exit 5: its JS leg passed 418/418, while its concurrent package-mode Python leg reported `Ran 0 tests in 0.000s` and `NO TESTS RAN` because `tests/__init__.py` and `tests/support/__init__.py` remain deleted by Sections 4.1–4.3/user work. The deletion was preserved. Equivalent explicit discovery, `zsh -o pipefail -c './.venv/bin/python -m unittest discover -s tests -p "test*.py" 2>&1 | rg "Ran [0-9]+ tests|^OK|^FAILED|NO TESTS"'` — exit 0; `Ran 756 tests in 11.010s`; `OK`.
  - `npm run build` — exit 0; CSS, vendor, Notes, and Tasks builds completed. Existing non-fatal Browserslist and Vite large-chunk advisories were emitted. Vendor, Notes, and `static/css/tailwind.css` stayed byte-identical; the known Tasks regeneration changed its hash from `52a40e8169f962213ae8087c648ad6fe82e622e5` to `36aa35fb14b891d22a75bd402574a48ab72598da` and was restored with path-scoped `git checkout-index --force -- static/js/tasks/dist/task-app.js`.
  - Exact CI-equivalent bundle block using `git ls-files --error-unmatch` and `git diff --exit-code` for all three paths — exit 0; `CI-equivalent generated frontend asset checks: PASS`. Final hashes are vendor `29c5277af79b309a191dca2f1984084fc59cc774`, Notes `33b917aff6e1340bcb1b7fb90ff95510b8a0d310`, and Tasks `52a40e8169f962213ae8087c648ad6fe82e622e5`.
  - `./.venv/bin/desloppify review --path .` — exit 127 because the target venv has no executable. The documented available-venv fallback `/Users/derekchen/Desktop/Nest.APStudy/.venv/bin/desloppify review --path .` from this worktree — exit 0, but reported `No completed scan metrics yet. Run: desloppify scan`; no unowned scanner-state generation was performed.
  - `git diff --check` — exit 0. Final `git diff --stat` covers the concurrent worktree as `23 files changed, 414 insertions(+), 129 deletions(-)`; the owned Section 4.4 numstat is 5/1 for CI, 2/1 for `.gitignore`, 27/0 for ESLint, 1/1 for `package.json`, and 24/24, 4/4, 2/2 for the three CSS files.
- Red/green or mutation evidence: The lint probe above is the required red/green proof for the enforced subset. No B608 findings were changed. CSS fallback search, ignore-path probes, and the three generated-asset checks all returned the required green results.
- Important implementation decisions: Kept the existing warning backlog visible and bounded instead of changing unrelated lint rules or mass-fixing warnings. Kept workflow tracking limited to YAML workflow files. Restored build-only Tasks churn because no assigned source change required a dist update. No repo-wide formatter or runtime dependency was introduced.
- Remaining risks or follow-ups: Literal `npm test` remains non-green solely because concurrent deletion of the two Python package initializers breaks default package discovery; explicit discovery is green at 756 tests. Desloppify cannot produce metrics in this worktree because its executable/state is unavailable; the fallback review completed without metrics. The lint backlog remains 3,283 warnings (including the 16 explicitly warning-baselined core unused-variable findings), and build advisories remain informational.
- Instructions for the next agent: Preserve the Section 4.1–4.3 concurrent paths and deleted initializers. Do not restore them as part of Section 4.4, do not fix B608 findings, and do not retain the regenerated Tasks bundle unless an owning source change requires it. No commit or push was made.
